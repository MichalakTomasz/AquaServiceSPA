import { ClickInfoDirective } from './click-info.directive';

describe('ClickInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickInfoDirective();
    expect(directive).toBeTruthy();
  });
});
